schema_version: 1
node_allocation:
  enabled: false
groups:
  example-team:
    gpu_quota: 8
    members: [alice]
    # ECP node identity; clusterx --include uses lowercase host-<dashed-ip>.
    nodes: [node-001]
  example-zero-quota:
    gpu_quota: 0
    cpu_quota: 14
    memory_quota_gib: 240
    members: [bob]
  default:
    gpu_quota: remainder
    members: []
